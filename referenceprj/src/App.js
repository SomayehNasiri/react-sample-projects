import React,{useState} from "react";
import Config from "./Config";
import TagsBuilder from "./TagsBuilder";

const App = () => {
  //   const [schedular,setSchedular]=useState(false);
  //   const [location,setLocation]=useState(false);
  //   const [tags,setTags]=useState([])
  // return (
  //   <div className="container">
  //     <Config title="Use Scheduler" toggleState={schedular} onToggle={val=>setSchedular(val)}/>
  //     <Config title="Use Location" toggleState={location} onToggle={val=>setLocation(val)}/>
  //     <TagsBuilder
  //       tags={tags}
  //       addTags={value => setTags([...tags,{id:new Date().getTime(),title:value}])}
  //     />
  //   </div>
  // );

  
};

export default App;
